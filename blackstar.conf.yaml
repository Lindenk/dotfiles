- defaults:
    link:
        create: true
        relink: true

# include common config
- shell:
    - 
        command: ./install desktop.conf.yaml
        stdout: true

# i3
- link:
    ~/.config/i3/config:
        path: blackstar/i3/config