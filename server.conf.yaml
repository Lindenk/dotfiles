- defaults:
    link:
        create: true
        relink: true

 # include common config
- shell:
    - 
        command: ./install common.conf.yaml
        stdout: true

- link:
    ~/.gnupg/gpg-agent.conf:
        path: server/gpg/.gnupg/gpg-agent.conf